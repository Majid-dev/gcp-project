<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Majid-Karimi Landing Page</title>
    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f9;
            color: #333;
        }
        h1, h2, h3, p {
            margin: 0;
            padding: 0;
        }

        /* Navigation Styles */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        nav a {
            text-decoration: none;
            color: #333;
            font-weight: bold;
            margin: 0 20px;
        }

        /* Hero Section */
        .hero {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-image: linear-gradient(135deg, #74ebd5 0%, #acb6e5 100%);
            color: white;
            text-align: center;
        }
        .hero h1 {
            font-size: 4rem;
            margin-bottom: 10px;
        }
        .hero p {
            font-size: 1.5rem;
            margin-bottom: 30px;
        }
        .hero button {
            padding: 15px 30px;
            font-size: 1.2rem;
            border: none;
            background-color: #ff8a00;
            color: white;
            border-radius: 50px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .hero button:hover {
            background-color: #ff6500;
        }

        /* Features Section */
        .features {
            padding: 80px 50px;
            display: flex;
            justify-content: space-between;
            background-color: #fff;
        }
        .feature-item {
            text-align: center;
            width: 30%;
        }
        .feature-item img {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
        }
        .feature-item h3 {
            margin-bottom: 10px;
            font-size: 1.5rem;
            color: #333;
        }
        .feature-item p {
            font-size: 1rem;
            color: #777;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 30px 0;
            background-color: #333;
            color: white;
        }
        footer p {
            font-size: 1rem;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <div class="logo">
            <h2>Majid Karimi</h2>
        </div>
        <div class="nav-links">
            <a href="#">Home</a>
            <a href="#">Features</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Welcome, {{ your_name }}!</h1>
        <p>This webpage was deployed automatically using Ansible on a GCP f1-micro instance.</p>
        <button>Get Started</button>
    </section>

    <!-- Footer -->
    <footer>
        <p>© All Rights Reserved.</p>
    </footer>

    <script>
        // JavaScript to add some interactivity
        document.querySelector("button").addEventListener("click", function() {
            alert("Welcome! Let's get started.");
        });
    </script>
</body>
</html>
